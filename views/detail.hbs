
<div class="col-md-12">
    <h3>{{title}}</h3>
</div>
<div class="col-md-12">
    <div class="col-md-7">
        <div><embed src="{{movie.flash}}" width="100%" height="380px"></embed></div>
        <div style="padding: 30px 0px;">
            {{#each comments}}
                <div style="padding:15px 0px; border-bottom: 1px solid #c1c1c1;">
                    <img style="vertical-align:top;height: 52px;width: 52px;border-radius: 26px;margin-right: 10px;" src="/images/m.jpg">
                    <div style="display: inline-block;">
                        <p style="color: blue;">{{username}}:</p>
                        <p>{{content}}</p>
                    </div>
                </div>
            {{/each}}
        </div>
        <div>
            <p>评论</p>
            <form class="form-horizontal" role="form" method="post" action="/comment/add">
                <input type="hidden" name="m_id" value="{{movie.id}}">
                <input type="hidden" name="m_from" value="{{movie.id}}">
                <input type="hidden" name="username" value="{{username}}">
                <div class="form-group">
                    <div class="col-sm-12">
                        <textarea style="width: 100%;" rows="5" cols="3" id="content" name="content"></textarea>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-10">
                        <button type="submit" class="btn btn-default">提交</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
    <div class="col-md-5">
        <dl>
            <dd>影片名称:{{movie.title}}</dd>
            <dd>影片国家:{{movie.country}}</dd>
            <dd>影片语言:{{movie.language}}</dd>
            <dd>影片年份:{{movie.year}}</dd>
            <dd>影片封面:<img src="{{movie.poster}}" alt="pic" style="width: 100px;"></dd>
            <dd>影片创建时间:{{movie.createTime}}</dd>
            <dd>影片更新时间:{{movie.updateTime}}</dd>
            <dd>影片价格:{{movie.price}}</dd>
            <dd>影片导演:{{movie.doctor}}</dd>
            <dd>影片描述:{{movie.summary}}</dd>
        </dl>
    </div>
</div>